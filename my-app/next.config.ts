const nextConfig = {
  output: "export",
  reactStrictMode: true,
  basePath: "/character-cards",
  assetPrefix: "/character-cards/",
  images: {
    unoptimized: true,
  }
};

export default nextConfig;